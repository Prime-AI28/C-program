#include <stdio.h>
#include<conio.h>

void main()
{
    char c; //variable c
    printf("Enter the character:");
    scanf("%c", &c); 
    // c = geat char();
    printf("%c\n",c);
    // putchar(c);
    printf("%d",c); // gives the ASCII value of variable c
}